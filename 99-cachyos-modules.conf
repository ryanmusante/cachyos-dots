# Load ntsync kernel module at boot
# /etc/modules-load.d/99-cachyos-modules.conf
#
# Required for PROTON_USE_NTSYNC=1 in Wine/Proton games.
# Provides Windows NT synchronization primitives for improved game performance.
# Requires kernel 6.13+ with CONFIG_NTSYNC enabled.
#
# Verify loaded: lsmod | grep ntsync

ntsync
